name: camera_module
services:
  camera_module:
    container_name: camera_module
    image: camera_module
    build:
      context: .
      dockerfile: Dockerfile
      tags:
        - camera_module:latest
        - camera_module:dev
    command: python -m camera_rest_node --port 2000
    privileged: true
    env_file: .env
    ports:
      - 2000:2000
